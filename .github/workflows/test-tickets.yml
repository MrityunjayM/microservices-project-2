name: tests-tickets

on:
  pull_request:
    paths: 
      - tickets/**

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: cd tickets && npm ci --include=dev && npm run test:ci
